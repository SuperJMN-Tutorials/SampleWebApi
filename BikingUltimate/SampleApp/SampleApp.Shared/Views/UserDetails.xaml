<UserControl
    x:Class="TestApp.Shared.Views.UserDetails"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TestApp.Shared.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:SampleApp.ViewModels"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400" d:DataContext="{d:DesignInstance viewModels:UserViewModel}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock FontWeight="Bold" HorizontalAlignment="Center" FontSize="20">
            <Run Text="{Binding FirstName}" />
            <Run Text="{Binding LastName}" />
        </TextBlock>
        <ListView Grid.Row="1" Header="Bicis" ItemsSource="{Binding Bikes}"></ListView>
    </Grid>
</UserControl>
